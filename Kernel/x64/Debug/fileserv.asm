; Listing generated by Microsoft (R) Optimizing Compiler Version 18.00.21005.1 

include listing.inc

INCLUDELIB LIBCMT
INCLUDELIB OLDNAMES

CONST	SEGMENT
$SG3958	DB	'Closing file %s ', 0dH, 0aH, 00H
CONST	ENDS
PUBLIC	?OpenFile@@YAHPEADH@Z				; OpenFile
PUBLIC	?ReadFile@@YA_KHPEAX_K@Z			; ReadFile
PUBLIC	?WriteFile@@YA_KHPEAX_K@Z			; WriteFile
PUBLIC	?CreateDir@@YAHPEAD@Z				; CreateDir
PUBLIC	?RemoveFile@@YAHPEAD@Z				; RemoveFile
PUBLIC	?CloseFile@@YAHH@Z				; CloseFile
PUBLIC	?FileIoControl@@YAHHHPEAX@Z			; FileIoControl
PUBLIC	?FileStat@@YAHHPEAX@Z				; FileStat
PUBLIC	?OpenDir@@YAHPEAD@Z				; OpenDir
PUBLIC	?ReadDir@@YAHHPEAX@Z				; ReadDir
PUBLIC	?ProcessGetFileDesc@@YAHPEBD@Z			; ProcessGetFileDesc
EXTRN	AuVFSOpen:PROC
EXTRN	AuVFSNodeIOControl:PROC
EXTRN	AuVFSFind:PROC
EXTRN	AuVFSNodeRead:PROC
EXTRN	AuVFSCreateDir:PROC
EXTRN	AuVFSCreateFile:PROC
EXTRN	?AuVFSRemoveFile@@YAHPEAU__VFS_NODE__@@0@Z:PROC	; AuVFSRemoveFile
EXTRN	?AuVFSRemoveDir@@YAHPEAU__VFS_NODE__@@0@Z:PROC	; AuVFSRemoveDir
EXTRN	AuVFSNodeWrite:PROC
EXTRN	AuGetCurrentThread:PROC
EXTRN	AuPmmngrAlloc:PROC
EXTRN	AuPmmngrFree:PROC
EXTRN	P2V:PROC
EXTRN	V2P:PROC
EXTRN	kfree:PROC
EXTRN	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z:PROC ; AuProcessFindThread
EXTRN	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z:PROC ; AuProcessFindSubThread
EXTRN	?AuProcessGetFileDesc@@YAHPEAU_au_proc_@@@Z:PROC ; AuProcessGetFileDesc
EXTRN	strcmp:PROC
EXTRN	memset:PROC
EXTRN	memcpy:PROC
EXTRN	SeTextOut:PROC
EXTRN	x64_cli:PROC
pdata	SEGMENT
$pdata$?OpenFile@@YAHPEADH@Z DD imagerel $LN12
	DD	imagerel $LN12+266
	DD	imagerel $unwind$?OpenFile@@YAHPEADH@Z
$pdata$?ReadFile@@YA_KHPEAX_K@Z DD imagerel $LN17
	DD	imagerel $LN17+477
	DD	imagerel $unwind$?ReadFile@@YA_KHPEAX_K@Z
$pdata$?WriteFile@@YA_KHPEAX_K@Z DD imagerel $LN16
	DD	imagerel $LN16+496
	DD	imagerel $unwind$?WriteFile@@YA_KHPEAX_K@Z
$pdata$?CreateDir@@YAHPEAD@Z DD imagerel $LN9
	DD	imagerel $LN9+182
	DD	imagerel $unwind$?CreateDir@@YAHPEAD@Z
$pdata$?RemoveFile@@YAHPEAD@Z DD imagerel $LN6
	DD	imagerel $LN6+108
	DD	imagerel $unwind$?RemoveFile@@YAHPEAD@Z
$pdata$?CloseFile@@YAHH@Z DD imagerel $LN10
	DD	imagerel $LN10+247
	DD	imagerel $unwind$?CloseFile@@YAHH@Z
$pdata$?FileIoControl@@YAHHHPEAX@Z DD imagerel $LN8
	DD	imagerel $LN8+189
	DD	imagerel $unwind$?FileIoControl@@YAHHHPEAX@Z
$pdata$?FileStat@@YAHHPEAX@Z DD imagerel $LN8
	DD	imagerel $LN8+274
	DD	imagerel $unwind$?FileStat@@YAHHPEAX@Z
$pdata$?OpenDir@@YAHPEAD@Z DD imagerel $LN10
	DD	imagerel $LN10+247
	DD	imagerel $unwind$?OpenDir@@YAHPEAD@Z
$pdata$?ReadDir@@YAHHPEAX@Z DD imagerel $LN11
	DD	imagerel $LN11+251
	DD	imagerel $unwind$?ReadDir@@YAHHPEAX@Z
$pdata$?ProcessGetFileDesc@@YAHPEBD@Z DD imagerel $LN11
	DD	imagerel $LN11+193
	DD	imagerel $unwind$?ProcessGetFileDesc@@YAHPEBD@Z
pdata	ENDS
xdata	SEGMENT
$unwind$?OpenFile@@YAHPEADH@Z DD 010d01H
	DD	0a20dH
$unwind$?ReadFile@@YA_KHPEAX_K@Z DD 011201H
	DD	0a212H
$unwind$?WriteFile@@YA_KHPEAX_K@Z DD 011201H
	DD	0c212H
$unwind$?CreateDir@@YAHPEAD@Z DD 010901H
	DD	08209H
$unwind$?RemoveFile@@YAHPEAD@Z DD 010901H
	DD	06209H
$unwind$?CloseFile@@YAHH@Z DD 010801H
	DD	08208H
$unwind$?FileIoControl@@YAHHHPEAX@Z DD 011101H
	DD	08211H
$unwind$?FileStat@@YAHHPEAX@Z DD 010d01H
	DD	0820dH
$unwind$?OpenDir@@YAHPEAD@Z DD 010901H
	DD	0a209H
$unwind$?ReadDir@@YAHHPEAX@Z DD 010d01H
	DD	0a20dH
$unwind$?ProcessGetFileDesc@@YAHPEBD@Z DD 010901H
	DD	08209H
xdata	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
i$1 = 32
currproc$ = 40
thr$ = 48
file$2 = 56
filename$ = 80
?ProcessGetFileDesc@@YAHPEBD@Z PROC			; ProcessGetFileDesc

; 409  : int ProcessGetFileDesc(const char* filename) {

$LN11:
	mov	QWORD PTR [rsp+8], rcx
	sub	rsp, 72					; 00000048H

; 410  : 	x64_cli();

	call	x64_cli

; 411  : 	AuThread* thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR thr$[rsp], rax

; 412  : 	if (!thr)

	cmp	QWORD PTR thr$[rsp], 0
	jne	SHORT $LN8@ProcessGet

; 413  : 		return -1;

	mov	eax, -1
	jmp	$LN9@ProcessGet
$LN8@ProcessGet:

; 414  : 	AuProcess* currproc = AuProcessFindThread(thr);

	mov	rcx, QWORD PTR thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR currproc$[rsp], rax

; 415  : 	if (!currproc){

	cmp	QWORD PTR currproc$[rsp], 0
	jne	SHORT $LN7@ProcessGet

; 416  : 		currproc = AuProcessFindSubThread(thr);

	mov	rcx, QWORD PTR thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR currproc$[rsp], rax

; 417  : 		if (!currproc)

	cmp	QWORD PTR currproc$[rsp], 0
	jne	SHORT $LN6@ProcessGet

; 418  : 			return -1;

	mov	eax, -1
	jmp	SHORT $LN9@ProcessGet
$LN6@ProcessGet:
$LN7@ProcessGet:

; 419  : 	}
; 420  : 	for (int i = 0; i < FILE_DESC_PER_PROCESS; i++) {

	mov	DWORD PTR i$1[rsp], 0
	jmp	SHORT $LN5@ProcessGet
$LN4@ProcessGet:
	mov	eax, DWORD PTR i$1[rsp]
	inc	eax
	mov	DWORD PTR i$1[rsp], eax
$LN5@ProcessGet:
	cmp	DWORD PTR i$1[rsp], 60			; 0000003cH
	jge	SHORT $LN3@ProcessGet

; 421  : 		AuVFSNode* file = currproc->fds[i];

	movsxd	rax, DWORD PTR i$1[rsp]
	mov	rcx, QWORD PTR currproc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$2[rsp], rax

; 422  : 		if (file) {

	cmp	QWORD PTR file$2[rsp], 0
	je	SHORT $LN2@ProcessGet

; 423  : 			if (strcmp(filename, file->filename) == 0) {

	mov	rax, QWORD PTR file$2[rsp]
	mov	rdx, rax
	mov	rcx, QWORD PTR filename$[rsp]
	call	strcmp
	test	eax, eax
	jne	SHORT $LN1@ProcessGet

; 424  : 				return i;

	mov	eax, DWORD PTR i$1[rsp]
	jmp	SHORT $LN9@ProcessGet
$LN1@ProcessGet:
$LN2@ProcessGet:

; 425  : 			}
; 426  : 		}
; 427  : 	}

	jmp	SHORT $LN4@ProcessGet
$LN3@ProcessGet:

; 428  : 
; 429  : 	return -1;

	mov	eax, -1
$LN9@ProcessGet:

; 430  : }

	add	rsp, 72					; 00000048H
	ret	0
?ProcessGetFileDesc@@YAHPEBD@Z ENDP			; ProcessGetFileDesc
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
current_proc$ = 32
fsys$ = 40
current_thr$ = 48
dirfile$ = 56
dire_$ = 64
dirfd$ = 96
dirent$ = 104
?ReadDir@@YAHHPEAX@Z PROC				; ReadDir

; 374  : int ReadDir(int dirfd, void* dirent) {

$LN11:
	mov	QWORD PTR [rsp+16], rdx
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 88					; 00000058H

; 375  : 	x64_cli();

	call	x64_cli

; 376  : 	if (!dirent)

	cmp	QWORD PTR dirent$[rsp], 0
	jne	SHORT $LN8@ReadDir

; 377  : 		return -1;

	mov	eax, -1
	jmp	$LN9@ReadDir
$LN8@ReadDir:

; 378  : 	if (dirfd == -1)

	cmp	DWORD PTR dirfd$[rsp], -1
	jne	SHORT $LN7@ReadDir

; 379  : 		return -1;

	mov	eax, -1
	jmp	$LN9@ReadDir
$LN7@ReadDir:

; 380  : 
; 381  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 382  : 	if (!current_thr){

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN6@ReadDir

; 383  : 		return 0;

	xor	eax, eax
	jmp	$LN9@ReadDir
$LN6@ReadDir:

; 384  : 	}
; 385  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 386  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN5@ReadDir

; 387  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 388  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN4@ReadDir

; 389  : 			return 0;

	xor	eax, eax
	jmp	SHORT $LN9@ReadDir
$LN4@ReadDir:
$LN5@ReadDir:

; 390  : 	}
; 391  : 
; 392  : 	AuDirectoryEntry* dire_ = (AuDirectoryEntry*)dirent;

	mov	rax, QWORD PTR dirent$[rsp]
	mov	QWORD PTR dire_$[rsp], rax

; 393  : 
; 394  : 	AuVFSNode* dirfile = current_proc->fds[dirfd];

	movsxd	rax, DWORD PTR dirfd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR dirfile$[rsp], rax

; 395  : 	if (!dirfile)

	cmp	QWORD PTR dirfile$[rsp], 0
	jne	SHORT $LN3@ReadDir

; 396  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN9@ReadDir
$LN3@ReadDir:

; 397  : 	AuVFSNode* fsys = (AuVFSNode*)dirfile->device;

	mov	rax, QWORD PTR dirfile$[rsp]
	mov	rax, QWORD PTR [rax+72]
	mov	QWORD PTR fsys$[rsp], rax

; 398  : 	if (!fsys)

	cmp	QWORD PTR fsys$[rsp], 0
	jne	SHORT $LN2@ReadDir

; 399  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN9@ReadDir
$LN2@ReadDir:

; 400  : 	if (fsys->read_dir)

	mov	rax, QWORD PTR fsys$[rsp]
	cmp	QWORD PTR [rax+168], 0
	je	SHORT $LN1@ReadDir

; 401  : 		return fsys->read_dir(fsys, dirfile, dire_);

	mov	r8, QWORD PTR dire_$[rsp]
	mov	rdx, QWORD PTR dirfile$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	mov	rax, QWORD PTR fsys$[rsp]
	call	QWORD PTR [rax+168]
$LN1@ReadDir:
$LN9@ReadDir:

; 402  : }

	add	rsp, 88					; 00000058H
	ret	0
?ReadDir@@YAHHPEAX@Z ENDP				; ReadDir
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
fd$ = 32
current_proc$ = 40
fsys$ = 48
current_thr$ = 56
dirfile$ = 64
filename$ = 96
?OpenDir@@YAHPEAD@Z PROC				; OpenDir

; 338  : int OpenDir(char* filename) {

$LN10:
	mov	QWORD PTR [rsp+8], rcx
	sub	rsp, 88					; 00000058H

; 339  : 	x64_cli();

	call	x64_cli

; 340  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 341  : 	if (!current_thr){

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN7@OpenDir

; 342  : 		return -1;

	mov	eax, -1
	jmp	$LN8@OpenDir
$LN7@OpenDir:

; 343  : 	}
; 344  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 345  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN6@OpenDir

; 346  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 347  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN5@OpenDir

; 348  : 			return -1;

	mov	eax, -1
	jmp	$LN8@OpenDir
$LN5@OpenDir:
$LN6@OpenDir:

; 349  : 	}
; 350  : 
; 351  : 	AuVFSNode *fsys = AuVFSFind(filename);

	mov	rcx, QWORD PTR filename$[rsp]
	call	AuVFSFind
	mov	QWORD PTR fsys$[rsp], rax

; 352  : 	AuVFSNode* dirfile = NULL;

	mov	QWORD PTR dirfile$[rsp], 0

; 353  : 	if (!fsys)

	cmp	QWORD PTR fsys$[rsp], 0
	jne	SHORT $LN4@OpenDir

; 354  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN8@OpenDir
$LN4@OpenDir:

; 355  : 	if (fsys->opendir)

	mov	rax, QWORD PTR fsys$[rsp]
	cmp	QWORD PTR [rax+96], 0
	je	SHORT $LN3@OpenDir

; 356  : 		dirfile = fsys->opendir(fsys, filename);

	mov	rdx, QWORD PTR filename$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	mov	rax, QWORD PTR fsys$[rsp]
	call	QWORD PTR [rax+96]
	mov	QWORD PTR dirfile$[rsp], rax
$LN3@OpenDir:

; 357  : 
; 358  : 	if (!dirfile)

	cmp	QWORD PTR dirfile$[rsp], 0
	jne	SHORT $LN2@OpenDir

; 359  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN8@OpenDir
$LN2@OpenDir:

; 360  : 
; 361  : 	int fd = AuProcessGetFileDesc(current_proc);

	mov	rcx, QWORD PTR current_proc$[rsp]
	call	?AuProcessGetFileDesc@@YAHPEAU_au_proc_@@@Z ; AuProcessGetFileDesc
	mov	DWORD PTR fd$[rsp], eax

; 362  : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN1@OpenDir

; 363  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN8@OpenDir
$LN1@OpenDir:

; 364  : 
; 365  : 	current_proc->fds[fd] = dirfile;

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rdx, QWORD PTR dirfile$[rsp]
	mov	QWORD PTR [rcx+rax*8+576], rdx

; 366  : 	return fd;

	mov	eax, DWORD PTR fd$[rsp]
$LN8@OpenDir:

; 367  : }

	add	rsp, 88					; 00000058H
	ret	0
?OpenDir@@YAHPEAD@Z ENDP				; OpenDir
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
status$ = 32
file$ = 40
current_proc$ = 48
current_thr$ = 56
fd$ = 80
buf$ = 88
?FileStat@@YAHHPEAX@Z PROC				; FileStat

; 305  : int FileStat(int fd, void* buf) {

$LN8:
	mov	QWORD PTR [rsp+16], rdx
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 72					; 00000048H

; 306  : 	x64_cli();

	call	x64_cli

; 307  : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN5@FileStat

; 308  : 		return -1;

	mov	eax, -1
	jmp	$LN6@FileStat
$LN5@FileStat:

; 309  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 310  : 	if (!current_thr){

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN4@FileStat

; 311  : 		return 0;

	xor	eax, eax
	jmp	$LN6@FileStat
$LN4@FileStat:

; 312  : 	}
; 313  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 314  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN3@FileStat

; 315  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 316  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN2@FileStat

; 317  : 			return 0;

	xor	eax, eax
	jmp	$LN6@FileStat
$LN2@FileStat:
$LN3@FileStat:

; 318  : 	}
; 319  : 	AuVFSNode* file = current_proc->fds[fd];

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$[rsp], rax

; 320  : 	if (!file)

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN1@FileStat

; 321  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN6@FileStat
$LN1@FileStat:

; 322  : 
; 323  : 	AuFileStatus *status = (AuFileStatus*)buf;

	mov	rax, QWORD PTR buf$[rsp]
	mov	QWORD PTR status$[rsp], rax

; 324  : 	status->current_block = file->current;

	mov	rax, QWORD PTR status$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	mov	ecx, DWORD PTR [rcx+56]
	mov	DWORD PTR [rax+9], ecx

; 325  : 	status->size = file->size;

	mov	rax, QWORD PTR file$[rsp]
	mov	eax, DWORD PTR [rax+32]
	mov	rcx, QWORD PTR status$[rsp]
	mov	QWORD PTR [rcx+1], rax

; 326  : 	status->filemode = file->flags;

	mov	rax, QWORD PTR status$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	movzx	ecx, BYTE PTR [rcx+64]
	mov	BYTE PTR [rax], cl

; 327  : 	status->eof = file->eof;

	mov	rax, QWORD PTR status$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	movzx	ecx, BYTE PTR [rcx+36]
	mov	BYTE PTR [rax+29], cl

; 328  : 	status->start_block = file->first_block;

	mov	rax, QWORD PTR status$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	mov	ecx, DWORD PTR [rcx+48]
	mov	DWORD PTR [rax+13], ecx

; 329  : 	status->user_id = 0;

	mov	rax, QWORD PTR status$[rsp]
	mov	DWORD PTR [rax+17], 0

; 330  : 	status->group_id = 0;

	mov	rax, QWORD PTR status$[rsp]
	mov	DWORD PTR [rax+21], 0

; 331  : 	return 0;

	xor	eax, eax
$LN6@FileStat:

; 332  : }

	add	rsp, 72					; 00000048H
	ret	0
?FileStat@@YAHHPEAX@Z ENDP				; FileStat
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
ret$ = 32
current_proc$ = 40
current_thr$ = 48
file$ = 56
fd$ = 80
code$ = 88
arg$ = 96
?FileIoControl@@YAHHHPEAX@Z PROC			; FileIoControl

; 276  : int FileIoControl(int fd, int code, void* arg) {

$LN8:
	mov	QWORD PTR [rsp+24], r8
	mov	DWORD PTR [rsp+16], edx
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 72					; 00000048H

; 277  : 	x64_cli();

	call	x64_cli

; 278  : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN5@FileIoCont

; 279  : 		return -1;

	mov	eax, -1
	jmp	$LN6@FileIoCont
$LN5@FileIoCont:

; 280  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 281  : 	if (!current_thr)

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN4@FileIoCont

; 282  : 		return 0;

	xor	eax, eax
	jmp	SHORT $LN6@FileIoCont
$LN4@FileIoCont:

; 283  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 284  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN3@FileIoCont

; 285  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 286  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN2@FileIoCont

; 287  : 			return 0;

	xor	eax, eax
	jmp	SHORT $LN6@FileIoCont
$LN2@FileIoCont:
$LN3@FileIoCont:

; 288  : 	}
; 289  : 	AuVFSNode* file = current_proc->fds[fd];

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$[rsp], rax

; 290  : 
; 291  : 	if (!file)

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN1@FileIoCont

; 292  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN6@FileIoCont
$LN1@FileIoCont:

; 293  : 
; 294  : 	int ret = 0;

	mov	DWORD PTR ret$[rsp], 0

; 295  : 	ret = AuVFSNodeIOControl(file, code, arg);

	mov	r8, QWORD PTR arg$[rsp]
	mov	edx, DWORD PTR code$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	call	AuVFSNodeIOControl
	mov	DWORD PTR ret$[rsp], eax

; 296  : 	return ret;

	mov	eax, DWORD PTR ret$[rsp]
$LN6@FileIoCont:

; 297  : }

	add	rsp, 72					; 00000048H
	ret	0
?FileIoControl@@YAHHHPEAX@Z ENDP			; FileIoControl
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
file$ = 32
current_proc$ = 40
current_thr$ = 48
fd$ = 80
?CloseFile@@YAHH@Z PROC					; CloseFile

; 241  : int CloseFile(int fd) {

$LN10:
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 72					; 00000048H

; 242  : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN7@CloseFile

; 243  : 		return 0;

	xor	eax, eax
	jmp	$LN8@CloseFile
$LN7@CloseFile:

; 244  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 245  : 	if (!current_thr)

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN6@CloseFile

; 246  : 		return 0;

	xor	eax, eax
	jmp	$LN8@CloseFile
$LN6@CloseFile:

; 247  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 248  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN5@CloseFile

; 249  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 250  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN4@CloseFile

; 251  : 			return 0;

	xor	eax, eax
	jmp	$LN8@CloseFile
$LN4@CloseFile:
$LN5@CloseFile:

; 252  : 	}
; 253  : 
; 254  : 	AuVFSNode* file = current_proc->fds[fd];

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$[rsp], rax

; 255  : 	if (file->flags & FS_FLAG_FILE_SYSTEM)

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 64					; 00000040H
	test	eax, eax
	je	SHORT $LN3@CloseFile

; 256  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN8@CloseFile
$LN3@CloseFile:

; 257  : 	if (file->flags & FS_FLAG_GENERAL)

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 4
	test	eax, eax
	je	SHORT $LN2@CloseFile

; 258  : 		kfree(file);

	mov	rcx, QWORD PTR file$[rsp]
	call	kfree
$LN2@CloseFile:

; 259  : 	
; 260  : 
; 261  : 	if (file->flags & FS_FLAG_DIRECTORY){

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 2
	test	eax, eax
	je	SHORT $LN1@CloseFile

; 262  : 		SeTextOut("Closing file %s \r\n", file->filename);

	mov	rax, QWORD PTR file$[rsp]
	mov	rdx, rax
	lea	rcx, OFFSET FLAT:$SG3958
	call	SeTextOut

; 263  : 		kfree(file);

	mov	rcx, QWORD PTR file$[rsp]
	call	kfree
$LN1@CloseFile:

; 264  : 	}
; 265  : 
; 266  : 	current_proc->fds[fd] = 0;

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	QWORD PTR [rcx+rax*8+576], 0

; 267  : 	return 0;

	xor	eax, eax
$LN8@CloseFile:

; 268  : }

	add	rsp, 72					; 00000048H
	ret	0
?CloseFile@@YAHH@Z ENDP					; CloseFile
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
dir$ = 32
fsys$ = 40
pathname$ = 64
?RemoveFile@@YAHPEAD@Z PROC				; RemoveFile

; 226  : int RemoveFile(char* pathname) {

$LN6:
	mov	QWORD PTR [rsp+8], rcx
	sub	rsp, 56					; 00000038H

; 227  : 	AuVFSNode* dir = AuVFSOpen(pathname);

	mov	rcx, QWORD PTR pathname$[rsp]
	call	AuVFSOpen
	mov	QWORD PTR dir$[rsp], rax

; 228  : 	if (!dir)

	cmp	QWORD PTR dir$[rsp], 0
	jne	SHORT $LN3@RemoveFile

; 229  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN4@RemoveFile
$LN3@RemoveFile:

; 230  : 	AuVFSNode* fsys = (AuVFSNode*)dir->device;

	mov	rax, QWORD PTR dir$[rsp]
	mov	rax, QWORD PTR [rax+72]
	mov	QWORD PTR fsys$[rsp], rax

; 231  : 	if (fsys->flags & FS_FLAG_DIRECTORY)

	mov	rax, QWORD PTR fsys$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 2
	test	eax, eax
	je	SHORT $LN2@RemoveFile

; 232  : 		return AuVFSRemoveDir(fsys, dir);

	mov	rdx, QWORD PTR dir$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	?AuVFSRemoveDir@@YAHPEAU__VFS_NODE__@@0@Z ; AuVFSRemoveDir
	jmp	SHORT $LN4@RemoveFile

; 233  : 	else

	jmp	SHORT $LN1@RemoveFile
$LN2@RemoveFile:

; 234  : 		return AuVFSRemoveFile(fsys, dir);

	mov	rdx, QWORD PTR dir$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	?AuVFSRemoveFile@@YAHPEAU__VFS_NODE__@@0@Z ; AuVFSRemoveFile
$LN1@RemoveFile:
$LN4@RemoveFile:

; 235  : }

	add	rsp, 56					; 00000038H
	ret	0
?RemoveFile@@YAHPEAD@Z ENDP				; RemoveFile
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
current_thr$ = 32
current_proc$ = 40
dirfile$ = 48
fsys$ = 56
filename$ = 80
?CreateDir@@YAHPEAD@Z PROC				; CreateDir

; 192  : int CreateDir(char* filename) {

$LN9:
	mov	QWORD PTR [rsp+8], rcx
	sub	rsp, 72					; 00000048H

; 193  : 	x64_cli();

	call	x64_cli

; 194  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 195  : 	if (!current_thr){

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN6@CreateDir

; 196  : 		return 0;

	xor	eax, eax
	jmp	$LN7@CreateDir
$LN6@CreateDir:

; 197  : 	}
; 198  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 199  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN5@CreateDir

; 200  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 201  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN4@CreateDir

; 202  : 			return 0;

	xor	eax, eax
	jmp	SHORT $LN7@CreateDir
$LN4@CreateDir:
$LN5@CreateDir:

; 203  : 	}
; 204  : 
; 205  : 	AuVFSNode *fsys = AuVFSFind(filename);

	mov	rcx, QWORD PTR filename$[rsp]
	call	AuVFSFind
	mov	QWORD PTR fsys$[rsp], rax

; 206  : 	AuVFSNode* dirfile = NULL;

	mov	QWORD PTR dirfile$[rsp], 0

; 207  : 	if (fsys){

	cmp	QWORD PTR fsys$[rsp], 0
	je	SHORT $LN3@CreateDir

; 208  : 		dirfile = AuVFSCreateDir(fsys, filename);

	mov	rdx, QWORD PTR filename$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	AuVFSCreateDir
	mov	QWORD PTR dirfile$[rsp], rax

; 209  : 	}
; 210  : 	else {

	jmp	SHORT $LN2@CreateDir
$LN3@CreateDir:

; 211  : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN7@CreateDir
$LN2@CreateDir:

; 212  : 	}
; 213  : 
; 214  : 	if (dirfile) {

	cmp	QWORD PTR dirfile$[rsp], 0
	je	SHORT $LN1@CreateDir

; 215  : 		kfree(dirfile);

	mov	rcx, QWORD PTR dirfile$[rsp]
	call	kfree

; 216  : 		return 0;

	xor	eax, eax
	jmp	SHORT $LN7@CreateDir
$LN1@CreateDir:

; 217  : 	}
; 218  : 
; 219  : 	return -1;

	mov	eax, -1
$LN7@CreateDir:

; 220  : }

	add	rsp, 72					; 00000048H
	ret	0
?CreateDir@@YAHPEAD@Z ENDP				; CreateDir
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
tv88 = 32
file$ = 40
current_proc$ = 48
buff$1 = 56
current_thr$ = 64
aligned_buffer$ = 72
fsys$ = 80
write_bytes$ = 88
fd$ = 112
buffer$ = 120
length$ = 128
?WriteFile@@YA_KHPEAX_K@Z PROC				; WriteFile

; 142  : size_t WriteFile(int fd, void* buffer, size_t length) {

$LN16:
	mov	QWORD PTR [rsp+24], r8
	mov	QWORD PTR [rsp+16], rdx
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 104				; 00000068H

; 143  : 	x64_cli();

	call	x64_cli

; 144  : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN11@WriteFile

; 145  : 		return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN11@WriteFile:

; 146  : 	if (!buffer)

	cmp	QWORD PTR buffer$[rsp], 0
	jne	SHORT $LN10@WriteFile

; 147  : 		return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN10@WriteFile:

; 148  : 	if (!length)

	cmp	QWORD PTR length$[rsp], 0
	jne	SHORT $LN9@WriteFile

; 149  : 		return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN9@WriteFile:

; 150  : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 151  : 	if (!current_thr)

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN8@WriteFile

; 152  : 		return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN8@WriteFile:

; 153  : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 154  : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN7@WriteFile

; 155  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 156  : 		if (!current_proc) 

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN6@WriteFile

; 157  : 			return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN6@WriteFile:
$LN7@WriteFile:

; 158  : 	}
; 159  : 	AuVFSNode* file = current_proc->fds[fd];

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$[rsp], rax

; 160  : 	uint8_t* aligned_buffer = (uint8_t*)buffer;

	mov	rax, QWORD PTR buffer$[rsp]
	mov	QWORD PTR aligned_buffer$[rsp], rax

; 161  : 	if (!file)

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN5@WriteFile

; 162  : 		return 0;

	xor	eax, eax
	jmp	$LN12@WriteFile
$LN5@WriteFile:

; 163  : 	size_t write_bytes = 0;

	mov	QWORD PTR write_bytes$[rsp], 0

; 164  : 	size_t ret_bytes;
; 165  : 	/* every general file will contain its
; 166  : 	* file system node as device */
; 167  : 	AuVFSNode* fsys = (AuVFSNode*)file->device;

	mov	rax, QWORD PTR file$[rsp]
	mov	rax, QWORD PTR [rax+72]
	mov	QWORD PTR fsys$[rsp], rax

; 168  : 
; 169  : 	if (file->flags & FS_FLAG_GENERAL && (!file->flags & FS_FLAG_TTY)) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 4
	test	eax, eax
	je	$LN4@WriteFile
	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	test	eax, eax
	jne	SHORT $LN14@WriteFile
	mov	DWORD PTR tv88[rsp], 1
	jmp	SHORT $LN15@WriteFile
$LN14@WriteFile:
	mov	DWORD PTR tv88[rsp], 0
$LN15@WriteFile:
	mov	eax, DWORD PTR tv88[rsp]
	and	eax, 256				; 00000100H
	test	eax, eax
	je	SHORT $LN4@WriteFile

; 170  : 		uint64_t* buff = (uint64_t*)P2V((size_t)AuPmmngrAlloc());

	call	AuPmmngrAlloc
	mov	rcx, rax
	call	P2V
	mov	QWORD PTR buff$1[rsp], rax

; 171  : 		memset(buff, 0, PAGE_SIZE);

	mov	r8d, 4096				; 00001000H
	xor	edx, edx
	mov	rcx, QWORD PTR buff$1[rsp]
	call	memset

; 172  : 		memcpy(buff,aligned_buffer, PAGE_SIZE);

	mov	r8d, 4096				; 00001000H
	mov	rdx, QWORD PTR aligned_buffer$[rsp]
	mov	rcx, QWORD PTR buff$1[rsp]
	call	memcpy

; 173  : 		AuVFSNodeWrite(fsys, file, buff, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR buff$1[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	AuVFSNodeWrite

; 174  : 		AuPmmngrFree((void*)V2P((size_t)buff));

	mov	rcx, QWORD PTR buff$1[rsp]
	call	V2P
	mov	rcx, rax
	call	AuPmmngrFree
$LN4@WriteFile:

; 175  : 	}
; 176  : 
; 177  : 	if (file->flags & FS_FLAG_TTY) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 256				; 00000100H
	test	eax, eax
	je	SHORT $LN3@WriteFile

; 178  : 		AuVFSNodeWrite(file, file, (uint64_t*)aligned_buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR aligned_buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	call	AuVFSNodeWrite
$LN3@WriteFile:

; 179  : 	}
; 180  : 
; 181  : 	if (file->flags & FS_FLAG_DEVICE) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 8
	test	eax, eax
	je	SHORT $LN2@WriteFile

; 182  : 		if (file->write) {

	mov	rax, QWORD PTR file$[rsp]
	cmp	QWORD PTR [rax+112], 0
	je	SHORT $LN1@WriteFile

; 183  : 			file->write(fsys, file, (uint64_t*)buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	mov	rax, QWORD PTR file$[rsp]
	call	QWORD PTR [rax+112]
$LN1@WriteFile:
$LN2@WriteFile:
$LN12@WriteFile:

; 184  : 		}
; 185  : 	}
; 186  : }

	add	rsp, 104				; 00000068H
	ret	0
?WriteFile@@YA_KHPEAX_K@Z ENDP				; WriteFile
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
file$ = 32
ret_bytes$ = 40
current_proc$ = 48
current_thr$ = 56
aligned_buffer$ = 64
fsys$ = 72
fd$ = 96
buffer$ = 104
length$ = 112
?ReadFile@@YA_KHPEAX_K@Z PROC				; ReadFile

; 87   : size_t ReadFile(int fd, void* buffer, size_t length) {

$LN17:
	mov	QWORD PTR [rsp+24], r8
	mov	QWORD PTR [rsp+16], rdx
	mov	DWORD PTR [rsp+8], ecx
	sub	rsp, 88					; 00000058H

; 88   : 	x64_cli();

	call	x64_cli

; 89   : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN14@ReadFile

; 90   : 		return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN14@ReadFile:

; 91   : 	if (!buffer)

	cmp	QWORD PTR buffer$[rsp], 0
	jne	SHORT $LN13@ReadFile

; 92   : 		return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN13@ReadFile:

; 93   : 	if (!length)

	cmp	QWORD PTR length$[rsp], 0
	jne	SHORT $LN12@ReadFile

; 94   : 		return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN12@ReadFile:

; 95   : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 96   : 	if (!current_thr)

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN11@ReadFile

; 97   : 		return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN11@ReadFile:

; 98   : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 99   : 	if (!current_proc){

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN10@ReadFile

; 100  : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 101  : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN9@ReadFile

; 102  : 			return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN9@ReadFile:
$LN10@ReadFile:

; 103  : 	}
; 104  : 	AuVFSNode* file = current_proc->fds[fd];

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rax, QWORD PTR [rcx+rax*8+576]
	mov	QWORD PTR file$[rsp], rax

; 105  : 	uint64_t* aligned_buffer = (uint64_t*)buffer;

	mov	rax, QWORD PTR buffer$[rsp]
	mov	QWORD PTR aligned_buffer$[rsp], rax

; 106  : 	if (!file)

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN8@ReadFile

; 107  : 		return 0;

	xor	eax, eax
	jmp	$LN15@ReadFile
$LN8@ReadFile:

; 108  : 	size_t ret_bytes = 0;

	mov	QWORD PTR ret_bytes$[rsp], 0

; 109  : 	
; 110  : 	/* every general file will contain its
; 111  : 	 * file system node as device */
; 112  : 	AuVFSNode* fsys = (AuVFSNode*)file->device;

	mov	rax, QWORD PTR file$[rsp]
	mov	rax, QWORD PTR [rax+72]
	mov	QWORD PTR fsys$[rsp], rax

; 113  : 	if (file->flags & FS_FLAG_GENERAL && !(file->flags & FS_FLAG_TTY)) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 4
	test	eax, eax
	je	SHORT $LN7@ReadFile
	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 256				; 00000100H
	test	eax, eax
	jne	SHORT $LN7@ReadFile

; 114  : 		ret_bytes = AuVFSNodeRead(fsys, file,aligned_buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR aligned_buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	AuVFSNodeRead
	mov	QWORD PTR ret_bytes$[rsp], rax
$LN7@ReadFile:

; 115  : 	}
; 116  : 	if (file->flags & FS_FLAG_DEVICE){

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 8
	test	eax, eax
	je	SHORT $LN6@ReadFile

; 117  : 		/* devfs will handle*/
; 118  : 		if (file->read)

	mov	rax, QWORD PTR file$[rsp]
	cmp	QWORD PTR [rax+104], 0
	je	SHORT $LN5@ReadFile

; 119  : 			ret_bytes = file->read(file, file, (uint64_t*)buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	mov	rax, QWORD PTR file$[rsp]
	call	QWORD PTR [rax+104]
	mov	QWORD PTR ret_bytes$[rsp], rax
$LN5@ReadFile:
$LN6@ReadFile:

; 120  : 	}
; 121  : 
; 122  : 	if (file->flags & FS_FLAG_TTY) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 256				; 00000100H
	test	eax, eax
	je	SHORT $LN4@ReadFile

; 123  : 		if (file->read)

	mov	rax, QWORD PTR file$[rsp]
	cmp	QWORD PTR [rax+104], 0
	je	SHORT $LN3@ReadFile

; 124  : 			ret_bytes = file->read(file, file, (uint64_t*)aligned_buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR aligned_buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	mov	rax, QWORD PTR file$[rsp]
	call	QWORD PTR [rax+104]
	mov	QWORD PTR ret_bytes$[rsp], rax
$LN3@ReadFile:
$LN4@ReadFile:

; 125  : 	}
; 126  : 	if ((file->flags & FS_FLAG_PIPE)) {

	mov	rax, QWORD PTR file$[rsp]
	movzx	eax, WORD PTR [rax+64]
	and	eax, 128				; 00000080H
	test	eax, eax
	je	SHORT $LN2@ReadFile

; 127  : 		/* ofcourse, pipe subsystem will handle */
; 128  : 		if (file->read)

	mov	rax, QWORD PTR file$[rsp]
	cmp	QWORD PTR [rax+104], 0
	je	SHORT $LN1@ReadFile

; 129  : 			ret_bytes = file->read(file, file, (uint64_t*)buffer, length);

	mov	r9d, DWORD PTR length$[rsp]
	mov	r8, QWORD PTR buffer$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	rcx, QWORD PTR file$[rsp]
	mov	rax, QWORD PTR file$[rsp]
	call	QWORD PTR [rax+104]
	mov	QWORD PTR ret_bytes$[rsp], rax
$LN1@ReadFile:
$LN2@ReadFile:

; 130  : 	}
; 131  : 
; 132  : 	return ret_bytes;

	mov	rax, QWORD PTR ret_bytes$[rsp]
$LN15@ReadFile:

; 133  : }

	add	rsp, 88					; 00000058H
	ret	0
?ReadFile@@YA_KHPEAX_K@Z ENDP				; ReadFile
_TEXT	ENDS
; Function compile flags: /Odtpy
; File e:\xeneva project\aurora\kernel\serv\fileserv.cpp
_TEXT	SEGMENT
fd$ = 32
current_proc$ = 40
file$ = 48
current_thr$ = 56
fsys$ = 64
filename$ = 96
mode$ = 104
?OpenFile@@YAHPEADH@Z PROC				; OpenFile

; 48   : int OpenFile(char* filename, int mode) {

$LN12:
	mov	DWORD PTR [rsp+16], edx
	mov	QWORD PTR [rsp+8], rcx
	sub	rsp, 88					; 00000058H

; 49   : 	x64_cli();

	call	x64_cli

; 50   : 	AuThread* current_thr = AuGetCurrentThread();

	call	AuGetCurrentThread
	mov	QWORD PTR current_thr$[rsp], rax

; 51   : 	if (!current_thr)

	cmp	QWORD PTR current_thr$[rsp], 0
	jne	SHORT $LN9@OpenFile

; 52   : 		return -1;

	mov	eax, -1
	jmp	$LN10@OpenFile
$LN9@OpenFile:

; 53   : 	AuProcess* current_proc = AuProcessFindThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindThread
	mov	QWORD PTR current_proc$[rsp], rax

; 54   : 	if (!current_proc) {

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN8@OpenFile

; 55   : 		current_proc = AuProcessFindSubThread(current_thr);

	mov	rcx, QWORD PTR current_thr$[rsp]
	call	?AuProcessFindSubThread@@YAPEAU_au_proc_@@PEAU_au_thread_@@@Z ; AuProcessFindSubThread
	mov	QWORD PTR current_proc$[rsp], rax

; 56   : 		if (!current_proc)

	cmp	QWORD PTR current_proc$[rsp], 0
	jne	SHORT $LN7@OpenFile

; 57   : 			return -1;

	mov	eax, -1
	jmp	$LN10@OpenFile
$LN7@OpenFile:
$LN8@OpenFile:

; 58   : 	}
; 59   : 
; 60   : 	AuVFSNode *fsys = AuVFSFind(filename);

	mov	rcx, QWORD PTR filename$[rsp]
	call	AuVFSFind
	mov	QWORD PTR fsys$[rsp], rax

; 61   : 	AuVFSNode* file = AuVFSOpen(filename);

	mov	rcx, QWORD PTR filename$[rsp]
	call	AuVFSOpen
	mov	QWORD PTR file$[rsp], rax

; 62   : 	if (!file) {

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN6@OpenFile

; 63   : 		if (mode & FILE_OPEN_CREAT || mode & FILE_OPEN_WRITE) {

	mov	eax, DWORD PTR mode$[rsp]
	and	eax, 8
	test	eax, eax
	jne	SHORT $LN4@OpenFile
	mov	eax, DWORD PTR mode$[rsp]
	and	eax, 4
	test	eax, eax
	je	SHORT $LN5@OpenFile
$LN4@OpenFile:

; 64   : 			file = AuVFSCreateFile(fsys, filename);

	mov	rdx, QWORD PTR filename$[rsp]
	mov	rcx, QWORD PTR fsys$[rsp]
	call	AuVFSCreateFile
	mov	QWORD PTR file$[rsp], rax

; 65   : 		}
; 66   : 		else 

	jmp	SHORT $LN3@OpenFile
$LN5@OpenFile:

; 67   : 			return -1;

	mov	eax, -1
	jmp	SHORT $LN10@OpenFile
$LN3@OpenFile:
$LN6@OpenFile:

; 68   : 	}
; 69   : 
; 70   : 	/* check for last time, if any error occured */
; 71   : 	if (!file)

	cmp	QWORD PTR file$[rsp], 0
	jne	SHORT $LN2@OpenFile

; 72   : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN10@OpenFile
$LN2@OpenFile:

; 73   : 
; 74   : 	int fd = AuProcessGetFileDesc(current_proc);

	mov	rcx, QWORD PTR current_proc$[rsp]
	call	?AuProcessGetFileDesc@@YAHPEAU_au_proc_@@@Z ; AuProcessGetFileDesc
	mov	DWORD PTR fd$[rsp], eax

; 75   : 	if (fd == -1)

	cmp	DWORD PTR fd$[rsp], -1
	jne	SHORT $LN1@OpenFile

; 76   : 		return -1;

	mov	eax, -1
	jmp	SHORT $LN10@OpenFile
$LN1@OpenFile:

; 77   : 	current_proc->fds[fd] = file;

	movsxd	rax, DWORD PTR fd$[rsp]
	mov	rcx, QWORD PTR current_proc$[rsp]
	mov	rdx, QWORD PTR file$[rsp]
	mov	QWORD PTR [rcx+rax*8+576], rdx

; 78   : 	return fd;

	mov	eax, DWORD PTR fd$[rsp]
$LN10@OpenFile:

; 79   : }

	add	rsp, 88					; 00000058H
	ret	0
?OpenFile@@YAHPEADH@Z ENDP				; OpenFile
_TEXT	ENDS
END
